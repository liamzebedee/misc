This plugin project is the test project for the emonic.base-plugin. 
It provides no end-user-functonality, but provides unit-tests 
to be run against the emonic.base-project for developers.

To start the unit tests, import this project in Your workspace, 
select the project in the navogator and select
"RunAs->JUnitPluginTest"and select in the opening dialog "AllTests"